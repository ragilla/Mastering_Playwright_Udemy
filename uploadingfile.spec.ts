import { test, expect } from '@playwright/test';
test('Uploading file', async ({ page }) => {
    await page.goto('https://the-internet.herokuapp.com/javascript_alerts');

    //setInputFiles(locator, uploadfilename1, uploadfilename2)

})